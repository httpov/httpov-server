<?php
$db["server"] = "localhost";
$db["db"] = "httpov";
$db["mortal"] = "httpov-db-username";
$db["mortal_pass"] = "httpov-db-password";

// Where to put finished batches, relative to the HTTPov directory:
// Set to "/finished/" to put them in the directory the status 
// viewer expect them to be in.
//$output_dir = "../safe_directory/";

// Whether to use the extended functionality of x.2 clients and
// the status viewer interface or not:
//$use_gui = 1;

// Whether to use syslog or not:
//$use_syslog = 1;
?>
